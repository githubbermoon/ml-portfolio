# Licensed webfonts

The page currently uses open-source substitutes so the repository remains distributable.

When licensed `.woff2` files are available, add them here and update the font declarations in `src/scss/_fonts.scss`:

- Neue Montreal · interface and metadata
- Louize · editorial text
- Mānuka · condensed display headings
- DaVinci · special display moments

Only add files covered by a webfont/self-hosting licence.
