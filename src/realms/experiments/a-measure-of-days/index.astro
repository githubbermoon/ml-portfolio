---
import { readFile } from 'node:fs/promises';
import { resolve } from 'node:path';

const document = await readFile(
  resolve(process.cwd(), 'public/experiments/a-measure-of-days/index.html'),
  'utf8',
);
---

<Fragment set:html={document} />
